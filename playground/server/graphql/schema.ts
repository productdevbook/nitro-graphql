// import { z } from 'zod'

export default defineSchema({
  // User: z.object({
  //   id: z.string(),
  //   name: z.string(),
  //   email: z.string().email(),
  //   age: z.number().min(0),
  // }),
})
