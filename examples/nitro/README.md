# Nitro GraphQL Starter

Minimal Nitro + GraphQL örneği.

## Kurulum

```bash
pnpm install
pnpm dev
```

GraphQL Playground: http://localhost:3000/api/graphql

## Örnek Sorgular

```graphql
query {
  hello
  greeting(name: "Dünya")
}
```
